source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in sequence_generator.gemspec
gemspec

#mention versions of gem
group :development, :test do
  gem 'pg'
  gem 'activemodel'
  gem "activesupport"
  gem "activerecord"
end